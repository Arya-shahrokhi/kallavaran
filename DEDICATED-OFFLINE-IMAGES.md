# Dedicated offline product images

Every seeded product points to its own local WebP asset under `frontend/public/images/products/dedicated/`. Product cards and detail pages are fully offline and do not depend on external image hosts.

Run the seed command again to update an existing database with the new image URLs.
